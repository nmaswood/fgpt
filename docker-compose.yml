version: "3.8"
services:
  db:
    image: postgres:14.4-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=fgpt
    ports:
      - "5432:5432"
    volumes:
      - db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 1s
      timeout: 5s
      retries: 5
  db_migration:
    image: db_migration
    build: ./db
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgres://postgres:postgres@db:5432/fgpt?sslmode=disable
  db_test_migration:
    image: db_migration
    build: ./db
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgres://postgres:postgres@db:5432/fgpt-test?sslmode=disable
  foo:
    image: test
    depends_on:
      - db
    environment:
      - SQL_URI=postgres://postgres:postgres@localhost:5432/fgpt?sslmode=disable
      - ML_SERVICE_URI=http://0.0.0.0:8001
      - JOB_TYPE=get-earnings-call-href

volumes:
  db:
    driver: local
